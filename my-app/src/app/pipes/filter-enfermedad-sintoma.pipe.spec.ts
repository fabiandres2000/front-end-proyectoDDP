import { FilterEnfermedadSintomaPipe } from './filter-enfermedad-sintoma.pipe';

describe('FilterEnfermedadSintomaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEnfermedadSintomaPipe();
    expect(pipe).toBeTruthy();
  });
});
