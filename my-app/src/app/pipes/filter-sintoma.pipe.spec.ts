import { FilterSintomaPipe } from './filter-sintoma.pipe';

describe('FilterSintomaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSintomaPipe();
    expect(pipe).toBeTruthy();
  });
});
