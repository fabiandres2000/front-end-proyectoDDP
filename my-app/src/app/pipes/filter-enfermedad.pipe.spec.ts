import { FilterEnfermedadPipe } from './filter-enfermedad.pipe';

describe('FilterEnfermedadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEnfermedadPipe();
    expect(pipe).toBeTruthy();
  });
});
